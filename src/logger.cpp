#include "logger.h"
#include <iostream> // Or replace with actual logging implementation later

void Logger::logDetection(const std::string& label, float score) {
    // Stub implementation: print to console
    // std::cout << "[Log] Detected: " << label << " (Score: " << score << ")" << std::endl;
    // No-op for now to avoid console spam
}

void Logger::logPerformance(double fps, double processingTime) {
    // Stub implementation: print to console
    // std::cout << "[Log] Performance: FPS=" << fps << ", ProcTime=" << processingTime << "s" << std::endl;
    // No-op for now
} 